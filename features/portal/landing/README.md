# Portal Landing

Feature pack for the `landing` module.

Current working assumption:

- Domain: `portal`
- Feature: `landing`

Change the domain folder later if the product uses a different domain name.
